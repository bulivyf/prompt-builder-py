=== Human-friendly Prompt ===

Write a Python function that uses the Sieve of Eratosthenes to find the three prime numbers closest to a given integer. The function should be fast, use minimal memory, and handle only integer inputs. It should also find the next prime number after a given number. Include comments explaining the algorithm.

=== LLM-optimized Prompt ===

Role: Developer
Goal: Implement a Python function that uses the Sieve of Eratosthenes to find the three prime numbers closest to a given integer. The function should also find the next prime number after a given number.
Constraints: The function must be optimized for speed and memory usage, handle only integer inputs, and include comments explaining the algorithm.
Output format: A Python function with appropriate comments.
Edge cases: Handle non-integer inputs by raising an exception.
